{
  "name": "@pokington/engine",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test ./test/*.test.mjs"
  },
  "dependencies": {
    "@pokington/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
